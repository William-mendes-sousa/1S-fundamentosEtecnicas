#include <stdio.h>

     int main () {
    
     int idade = 20;
     double altura = 1.80;

         if (idade >= 18 && idade <= 30 && altura >= 1.80)
         {
            printf ("você esta dentro da faixa");
         } else {
            printf ("você esta fora do padrão da empresa");
         }
         
    





     return 0; 
        }
#include <stdio.h>

     int main () {

    int a = 10;
    int b = 20;
    
    printf("a > b: %d\n", a > b);        // se A for maior que B o resultado é 1, senao é 0 
    printf("a < b: %d\n", a < b);        // se B for menor que A o resultado é 1, senao é 0 
    printf("a == b: %d\n", a == b);      // Se A for igual a B o reusltado é 1, senao é 0
    printf("a != b: %d\n", a != b);      // se A for diferente de B o resultado é 1, senão é 0

    

return 0;

}